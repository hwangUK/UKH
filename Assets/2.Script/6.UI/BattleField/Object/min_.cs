using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class min_ : MonoBehaviour {

    
	// Update is called once per frame
	void Update () {
       // this.GetComponent<Text>().text = transform.parent.parent.GetComponent<HUD_Add_Resource>().Obj_Now_Resource.ToString();
	}
}
